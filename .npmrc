# engine-strict is disabled because tooling requires Node >=20.19 while some sandboxes
# pin 20.18. CI (actions/setup-node with .nvmrc "20") installs the latest 20.x, which
# satisfies every engines constraint. Bump to `true` once all environments are >=20.19.
engine-strict=false
auto-install-peers=true
