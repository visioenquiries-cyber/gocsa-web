// Root ESLint flat config — re-exports the shared base (@gocsa/config).
// Package/app-level eslint.config.mjs files extend this and may add framework rules.
export { default } from "@gocsa/config/eslint";
